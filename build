username="fonzzy1"

docker buildx create --use

docker buildx build --platform linux/amd64,linux/arm64 . -t "${username}/vim" --push

if [ "$(git rev-parse --abbrev-ref HEAD)" = "master" ] && [ "$(git status --porcelain)" = "" ]; then
  docker push "${username}/vim"
fi
