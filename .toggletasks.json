{
    "tasks": [
        {
            "name": "Run build script",
            "cmd": "sh ./build"
        },
        {
            "name": "Login to Docker",
            "cmd": "docker login -u fonzzy1"
        }
    ]
}
